*Обоснование решений — в [design.md](design.md): схема таблицы, почему занятое имя ловится индексом, почему правила пароля вынесены в отдельный класс и почему заглушка удаляется целиком. Изменение опирается на PostgreSQL и Flyway из `store-file-listing-in-postgres`: перед началом убедитесь, что то изменение на месте и `./mvnw test` зелёный. `JAVA_HOME` должен указывать на JDK 25, база поднимается `docker compose up -d`.*

## 1. Зависимость и схема

- [x] 1.1 Зафиксировать базовую линию: `./mvnw test`, записать фактическое число проходящих тестов (ожидается 109 после `store-file-listing-in-postgres`), чтобы потом было с чем сравнивать
- [x] 1.2 Добавить в `pom.xml` зависимость `org.springframework.security:spring-security-crypto` без версии — ею управляет `spring-security-bom`, импортированный BOM-ом Boot 4.1.1. Проверить сборкой, что версия разрешилась и что Spring Security целиком за собой не притянулся
- [x] 1.3 Создать миграцию `V2__create_app_user.sql` с таблицей `app_user` и уникальным индексом по `lower(username)` — DDL в [design.md](design.md). Проверить на пустой базе: таблица и индекс созданы, `username` и `password_hash` не допускают `null`
- [x] 1.4 Проверить, что индекс действительно регистронезависим: вставить вручную двух пользователей, чьи имена отличаются только регистром, и убедиться, что вторая вставка отвергнута базой

## 2. Хранение пользователя

- [x] 2.1 Добавить подпакет `auth.repository` и запись `AppUserRow(String id, String username, String passwordHash, Instant createdAt)` с `@Id` на `id`
- [x] 2.2 Добавить `AppUserRepository` с явным `@Modifying @Query` для вставки (не `save()` — причина в [design.md](design.md)), не перечисляющим `created_at`, и методом поиска по имени без учёта регистра (`where lower(username) = lower(:username)`)
- [x] 2.3 Написать тест репозитория на реальной базе: вставка и чтение возвращают то же самое; поиск находит пользователя по имени в любом регистре; вставка имени, отличающегося только регистром, падает с нарушением уникальности

## 3. Правила пароля

- [x] 3.1 Добавить `PasswordPolicy` в `auth.service`: метод возвращает список невыполненных правил (длина не меньше восьми, заглавная, строчная, цифра, специальный символ как «не буква и не цифра»). Константы правил и текстов — в `AuthMessages`, инлайн-строк в аргументах вызовов быть не должно
- [x] 3.2 Добавить `WeakPasswordException`, несущую список нарушений, и обработку в `AuthExceptionHandler`: `400 Bad Request`, в теле перечислены все нарушенные правила, присланного пароля в ответе нет
- [x] 3.3 Написать юнит-тест `PasswordPolicy` без веб-слоя: пароль ровно восьми символов со всеми четырьмя видами символов проходит; семь символов не проходят; отсутствие каждого из четырёх видов ловится по отдельности; пароль, нарушающий несколько правил, даёт все нарушения сразу, а не первое

## 4. Регистрация

- [x] 4.1 Добавить в `auth.model` записи `RegisterRequest(String username, String password)` с `@NotBlank` и `@Size` по границам `LoginRequest` и переопределённым `toString`, скрывающим пароль, и `RegisterResponse(String id, String username)`. Проверить тестом, что `toString` пароль не печатает
- [x] 4.2 Добавить интерфейс `UserRegistry` и его реализацию `UserRegistrationService` в `auth.service`: проверяет правила пароля, хеширует BCrypt, генерирует id, вставляет строку; `DuplicateKeyException` от индекса превращает в `UsernameAlreadyTakenException`. Проверки «есть ли уже такое имя» перед вставкой быть не должно
- [x] 4.3 Объявить `BCryptPasswordEncoder` бином в `auth.config` со стойкостью из свойства `app.auth.password.bcrypt-strength` (по умолчанию 10) и записать рядом, что занижать её допустимо только в тестах
- [x] 4.4 Добавить `POST /api/auth/register` в `AuthController`: `201 Created` с id и именем; `UsernameAlreadyTakenException` — `409 Conflict` через `AuthExceptionHandler`. Имя сохраняется в том регистре, в каком его прислали
- [x] 4.5 Исключить `/api/auth/register` из перехватчика в `AuthWebConfig` рядом с `/api/auth/login`
- [x] 4.6 Написать тесты эндпоинта: успешная регистрация отдаёт `201` с id и именем и не содержит пароля; регистрация без токена проходит; занятое имя и имя в другом регистре дают `409`; пустые и слишком длинные значения дают `400`; слабый пароль даёт `400` с перечислением правил

## 5. Настоящая проверка учётных данных

- [x] 5.1 Добавить `StoredUserCredentialsVerifier implements CredentialsVerifier` в `auth.service`: ищет пользователя по имени без учёта регистра и сверяет пароль с хешем; при отсутствии пользователя всё равно выполняет сверку с фиксированным хешем, чтобы время ответа не выдавало существование имени
- [x] 5.2 **BREAKING**: удалить `AlwaysApprovingCredentialsVerifier`, `ProductionCredentialsVerifierConfiguration`, константу `AuthMessages.NO_REAL_CREDENTIALS_VERIFIER` и тест `ProductionProfileTest`. Проверить `grep`, что ни на один из них не осталось ссылок
- [x] 5.3 Написать тесты верификатора: верный пароль принимается, неверный отвергается, незарегистрированное имя отвергается, имя в другом регистре принимается
- [x] 5.4 Написать интеграционный тест входа: после регистрации вход тем же паролем даёт `200` с токеном; неверный пароль и незарегистрированное имя дают `401`, причём оба ответа совпадают по статусу и телу

## 6. Починка существующих тестов

- [x] 6.1 Научить `AuthenticatedApiTest` регистрировать пользователя перед первым входом, через HTTP, а не вставкой в базу. Пароль подобрать удовлетворяющим правилам. Проверить, что наследники (`AuthEnforcementTest`, `ExistingEndpointsRequireTokenTest`, `TokenExpiryIntegrationTest`, `AuthenticatedUserIntegrationTest`, все тесты `filestorage`) снова зелёные
- [x] 6.2 Поправить `AuthLoginEndpointTest`, отправляющий собственный запрос на вход: сценарий успешного входа должен сначала зарегистрировать пользователя; сценарии `400` про пустые поля остаются как есть
- [x] 6.3 Поправить `FileStorageMultipartEncodingTest` и `FileStorageUploadLimitsTest`, у которых свой вход: тот, что логинится, должен сначала зарегистрироваться. `FileStorageUploadLimitsTest` намеренно ходит без токена — там менять нечего, кроме подъёма контекста
- [x] 6.4 Занизить стойкость BCrypt в тестовой конфигурации и убедиться, что общее время `./mvnw test` не выросло существенно относительно базовой линии из задачи 1.1
- [x] 6.5 Проверить `AuthControllerTest`. От удаления заглушки он не зависит — мокает `CredentialsVerifier`, — но правка всё же понадобилась: у `AuthController` появился третий параметр конструктора, поэтому в тест добавлен мок `UserRegistry`. Утверждения не менялись

## 7. Проверка

- [x] 7.1 `./mvnw clean test` — зелено; сверить поклассовые числа с базовой линией из задачи 1.1 и объяснить каждое расхождение: удалённый `ProductionProfileTest`, новые тесты, изменённые классы
- [x] 7.2 Проверить вручную на запущенном приложении: регистрация отдаёт `201` с id и именем; повторная регистрация того же имени — `409`; вход этим паролем — `200` с токеном; вход неверным паролем — `401`
- [x] 7.3 Проверить, что в базе нет открытого пароля: посмотреть строку в `app_user` и убедиться, что там хеш BCrypt, а не присланное значение; зарегистрировать двух пользователей с одинаковым паролем и убедиться, что хеши разные
- [x] 7.4 Проверить, что пароль не попадает в лог: выполнить регистрацию со слабым паролем и вход с неверным паролем при повышенной подробности логирования и убедиться, что присланное значение нигде не встречается
- [x] 7.5 Проверить, что регистрация и вход не требуют токена, а все прочие эндпоинты по-прежнему отвечают `401` без него и несут заголовок `WWW-Authenticate`
- [x] 7.6 Проверить, что приложение стартует под профилем `prod`: раньше это было невозможно из-за запрета на заглушку, теперь должно работать при доступной базе
- [x] 7.7 Проверить рабочее дерево: `git status` показывает только намеренные правки, удалённые классы действительно удалены, в `application.properties` не появилось паролей

## 1. Владелец в схеме и репозитории

- [x] 1.1 Добавить миграцию `V3__add_stored_file_owner.sql`: колонка `owner_username varchar(100)` и индекс `stored_file_owner_idx on stored_file (lower(owner_username), created_at, id)`; проверка — `./mvnw test` со стартом контекста на `PostgresTestContainer` проходит, а в поднятой базе колонка и индекс есть
- [x] 1.2 Добавить `ownerUsername` в `StoredFileRow` и в `forInsert`; проверка — существующий `StoredFileRepositoryTest` компилируется и проходит
- [x] 1.3 Расширить `StoredFileRepository`: вставка с владельцем и `findAllOrderedByOwner` с `where lower(owner_username) = lower(:owner)`; проверка — новые случаи в `StoredFileRepositoryTest`: строка чужого владельца и строка без владельца в выборку не попадают, а имя в другом регистре попадает

## 2. Владелец в доменном сервисе и API

- [x] 2.1 Изменить интерфейс `FileStorage`: `store(MultipartFile, String owner)`, `listFiles(String owner)`, `load(String id, String owner)` с описанием в javadoc, что чужой файл неотличим от отсутствующего; проверка — интерфейс компилируется, javadoc называет условия исключений
- [x] 2.2 Реализовать владельца в `FileStorageService`: запись владельца при `store`, выборка по владельцу в `listFiles`, проверка владельца в `load` с `StoredFileNotFoundException` для чужой и бесхозной строки; проверка — обновлённый `FileStorageServiceTest` со случаями «свой», «чужой», «без владельца», «имя в другом регистре»
- [x] 2.3 Провести владельца через `FileStorageController`: `AuthenticatedUser.username(request)` в трёх методах; проверка — `FileStorageControllerTest` проходит, загрузка без токена по-прежнему даёт `401`
- [x] 2.4 Обновить `InMemoryStoredFileRepository` и прочие тестовые двойники под новую сигнатуру; проверка — `./mvnw test` компилирует тестовый исходный набор без ошибок
- [x] 2.5 Добавить интеграционный тест изоляции: два зарегистрированных пользователя, каждый видит в `GET /api/files` только свои файлы, `GET /api/files/{id}` на чужой id даёт `404` с тем же телом, что и несуществующий id; проверка — новый тест на базе `AuthenticatedApiTest` проходит
- [x] 2.6 Добавить тест на файл без владельца: строка с `owner_username is null` не появляется в списке и не скачивается ни по чьему токену; проверка — тест проходит

## 3. Экран входа

- [x] 3.1 Создать `src/main/resources/static/login.html` и `app.css`: форма с полями имени и пароля, кнопкой отправки и местом для сообщения об ошибке; проверка — страница открывается по `http://localhost:8080/login.html` и не требует токена
- [x] 3.2 Создать `api.js`: чтение и запись токена в `sessionStorage`, обёртка `request()` с заголовком `Authorization`, обработка `401` (забыть токен, `location.replace('/login.html')`), русские тексты сообщений одним модулем; проверка — вызов защищённого эндпоинта без токена уводит на логин
- [x] 3.3 Создать `login.js`: проверка непустых полей до отправки, `POST /api/auth/login`, сохранение токена, переход на `/`; блокировка повторной отправки до ответа; проверка — вход с верными данными открывает список, двойное нажатие шлёт один запрос
- [x] 3.4 Показ ошибок входа: `401` — «имя или пароль неверны» без указания, существует ли имя; `400` — ошибка ввода; сеть и `5xx` — «вход сейчас невозможен», введённое имя остаётся в форме, пароль не сохраняется и не попадает в адрес; проверка — все четыре случая воспроизведены вручную по чек-листу задачи 5.1

## 4. Страница списка файлов

- [x] 4.1 Создать `index.html`: шапка с кнопкой «Выйти», форма загрузки, таблица файлов, область предпросмотра; проверка — разметка открывается и без токена не содержит данных файлов
- [x] 4.2 Создать `files.js` со списком: проверка токена на загрузке и на `pageshow`, `GET /api/files`, строки с именем, типом и размером через `textContent`, пустой список — пояснение, ошибка не-`401` — сообщение с повтором; проверка — три состояния (файлы есть, файлов нет, сервис отвечает ошибкой) воспроизведены вручную
- [x] 4.3 Реализовать предпросмотр: `fetch` с токеном → `Blob`, текстовые типы в `<pre>` через `textContent`, `image/*` через objectURL в `<img>` с `revokeObjectURL` при смене файла; проверка — текстовый файл и картинка показываются, разметка внутри файла не исполняется
- [x] 4.4 Реализовать отказы предпросмотра: неподдерживаемый тип и файл больше порога в 1 МБ дают сообщение и оставляют кнопку «Скачать»; ошибка загрузки содержимого показывается в области предпросмотра, список остаётся; проверка — три случая воспроизведены вручную
- [x] 4.5 Реализовать скачивание: `fetch` с токеном → `Blob` → `<a download>` с именем из метаданных, затем `revokeObjectURL`; проверка — файл сохраняется под исходным именем, включая имя с не-ASCII символами
- [x] 4.6 Реализовать загрузку: `POST /api/files` с токеном, обновление списка после `201`, сообщение при невыбранном файле, при `400` и при `413`, признак идущей загрузки без повторной отправки; проверка — загрузка обычного файла и файла больше 10 МБ воспроизведены вручную
- [x] 4.7 Реализовать выход: очистка токена, списка и предпросмотра, `location.replace('/login.html')`; возврат кнопкой «назад» не показывает данные; проверка — воспроизведено вручную, включая возврат из кеша браузера

## 5. Проверка и документация

- [x] 5.1 Пройти чек-лист страниц целиком в браузере: вход, список, предпросмотр текста и картинки, тип без предпросмотра, скачивание, загрузка, выход, истёкший токен — все пункты пройдены на запущенном сервисе. Результат: пустые поля формы не отправляют запрос; `401` даёт «Имя пользователя или пароль неверны», имя остаётся, пароль стирается, адрес чист; тройное нажатие «Войти» шлёт один запрос; список показывает имя, тип и размер; пустой список даёт «Файлов пока нет»; ошибка `503` на списке даёт сообщение и кнопку «Повторить», не выкидывая на логин; текст показывается как текст (`<b>` не становится разметкой), `image/png` — как изображение, `application/octet-stream` — «Предпросмотр… недоступен»; файл в 1.3 МБ даёт «Файл слишком велик…» и содержимое не запрашивается; скачивание шлёт `GET /api/files/{id}` с токеном и не оставляет ссылок в DOM; загрузка без файла, пустого файла (`400`) и файла в 11 МБ (`413`) даёт три разных сообщения; выход и возврат «назад» приводят на логин без данных; недействительный токен уводит на логин с «Сеанс закончился, войдите снова». Найдено и исправлено: на узком экране таблица обрезала кнопки — добавлен `.table-scroll` с `overflow-x: auto`, а перенос имени файла переведён с `word-break: break-all` на `overflow-wrap: anywhere`
- [x] 5.2 Проверить изоляцию вручную: воспроизведено на запущенном сервисе — учётные записи `demo` и `other` видят каждая только свои файлы, а чужой id в предпросмотре даёт «Файла больше нет», список при этом остаётся на месте и из сеанса не выбрасывает
- [x] 5.3 Обновить `README.md`: раздел про веб-страницы (`/login.html` и `/`), владелец файла в таблице эндпоинтов, поведение файлов без владельца; проверка — описанное совпадает с поведением после задач 1–4
- [x] 5.4 Прогнать `./mvnw test` и убедиться, что сборка чистая; проверка — команда завершается успешно
